setenv LLVM_CC_NAME clang${LLVM_SUFFIX}

setenv LLVM_CXX_NAME clang++${LLVM_SUFFIX}

setenv LLVM_LINK_NAME llvm-link${LLVM_SUFFIX}

setenv LLVM_AR_NAME llvm-ar${LLVM_SUFFIX}

setenv LLVM_AS_NAME llvm-as${LLVM_SUFFIX}

setenv LLVM_LD_NAME llvm-ld${LLVM_SUFFIX}

setenv LLVM_LLC_NAME llc${LLVM_SUFFIX}

setenv LLVM_OPT_NAME opt${LLVM_SUFFIX}

setenv LLVM_NM_NAME llvm-nm${LLVM_SUFFIX}

setenv LLVM_CPP_NAME clang-cpp${LLVM_SUFFIX}
