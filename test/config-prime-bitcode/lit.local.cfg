# -*- Python -*-

import os
import sys
import re
import platform

test_exec_root = os.path.abspath (config.test_exec_root)

config.suffixes = ['.ll']
config.excludes = []

# define directory where test source is located
config.substitutions.append(('%intra-1', os.path.join(test_exec_root, 'intra-1')))
config.substitutions.append(('%intra-2', os.path.join(test_exec_root, 'intra-2')))
config.substitutions.append(('%intra-3', os.path.join(test_exec_root, 'intra-3')))
config.substitutions.append(('%intra-4', os.path.join(test_exec_root, 'intra-4')))
config.substitutions.append(('%intra-5', os.path.join(test_exec_root, 'intra-5')))
