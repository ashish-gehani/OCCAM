# -*- Python -*-

import os
import sys
import re
import platform

config.suffixes = ['.c']
config.excludes = []
config.substitutions.append(('%cmd', os.path.join(config.test_source_root, 'ipdse', 'run.sh')))
