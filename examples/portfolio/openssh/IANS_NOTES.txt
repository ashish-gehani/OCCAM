This one is delicate.

This was required on linux:

sudo mkdir /var/empty

For running the server is user space

http://cygwin.com/ml/cygwin/2008-04/msg00363.html


the idea of this example will be to specialize the client and server while still
getting them to talk to one another,


clang++ ssh.bc libcrypto.a.bc libz.a.bc -ldl -lresolv -o ssh_from_bc

