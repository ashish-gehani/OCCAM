Specialization of nweb with initial program arguments using the 'nweb.manifest'.

The 'build.sh' script builds the nweb binary which serves files out of the subdirectory 'root'.

To test the result, run

	./nweb 8181 ./root &

and connect with

	wget localhost:8181
